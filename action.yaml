name: Action Template
description: An opinionated template for creating GitHub Actions

inputs:
  who-to-greet:
    description: Who to greet
    required: true
    default: World

author: luxass
branding:
  color: purple
  icon: package

runs:
  using: node20
  main: dist/index.js
